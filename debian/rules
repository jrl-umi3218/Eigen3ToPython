#!/usr/bin/make -f

#DH_VERBOSE = 1

export PYBUILD_NAME=eigen
export PYBUILD_INSTALL_ARGS_python3=--install-layout=deb

%:
	dh $@ --with python2,python3 --buildsystem=pybuild

override_dh_auto_test:
	nosetests
